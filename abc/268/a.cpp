#include <bits/stdc++.h>
using namespace std;

int main() {

    int a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;

    set<int> s = {a, b, c, d, e};
    
    cout << s.size() << endl;

    return 0;
}