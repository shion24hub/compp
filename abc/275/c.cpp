#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)

int main() {

    vector<string> s(9);
    rep(i, 9) cin >> s[i];

    for (int i = 0; i < 9; i++) {
        for (int j = 0; j < 9; j++) {
            if (s[i][j] == '#') {
                
            }
        }
    }

    return 0;
}